import { describe, it, expect } from 'vitest';

// Tests disabled - localChartsStorage exports are commented out
// These tests will be re-enabled when the storage service is restored

describe('LocalChartsStorage Service', () => {
    it.skip('should save a chart and retrieve it', () => {
        // Test disabled - storage functions are commented out
        expect(true).toBe(true);
    });
});
